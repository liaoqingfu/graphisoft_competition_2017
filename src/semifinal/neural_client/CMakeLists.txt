project(NeuralClient)
include_directories(${NeuralNetwork_SOURCE_DIR})
include_directories(${GeneticLearning_SOURCE_DIR})
include_directories(${Matrix_SOURCE_DIR})
include_directories(${Track_SOURCE_DIR})
include_directories(${Client_SOURCE_DIR})
include_directories(${Game_SOURCE_DIR})
include_directories(${cpp-util_SOURCE_DIR}/include)

add_executable(neural_client main.cpp)
target_link_libraries(neural_client NeuralNetwork client track Matrix)
target_link_libraries(neural_client ${Boost_LIBRARIES})

add_executable(learner learner.cpp)
target_link_libraries(learner NeuralNetwork GeneticLearning game client track Matrix cpp-util)
target_link_libraries(learner ${Boost_LIBRARIES})
